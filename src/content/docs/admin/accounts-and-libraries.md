---
title: Accounts and libraries
description: Roles, separate libraries, and inviting other people to a library.
---

## Roles

Every account has one of two roles:

- **Admin.** Manages sources, libraries, and other accounts, and can see operational
  information such as the activity log and diagnostics.
- **Reader.** Browses and reads, and manages their own reading progress, bookmarks,
  and highlights. A reader cannot change sources or library settings. If you allow it
  for a library, a reader can also add works to that library from a source.

The first account, created during [setup](/docs/getting-started/first-run-setup/), is
an admin. There is no owner role above admin: every admin has the same access.

## Libraries

One running Alexandryn can hold more than one library. That is useful when a household
wants separate collections, or different permissions on the same server.

To create a library beyond the default one, open the library management screen
(admin only) and give it a name and an optional description. There you also choose
whether readers can add works from a source, or only admins can.

## Invite someone

From a library's management screen, create an invitation with the person's email
address and a role, admin or reader. They accept it from a link. Accepting creates
their account if they do not have one, or adds them to the library if they do.

## One library at a time

Each request works against one active library. Switching libraries in the interface
changes which one your next actions apply to. It does not search or merge across
libraries.
